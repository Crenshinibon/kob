name: Cleanup stale Neon preview branches

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'List branches to delete without deleting'
        type: boolean
        default: true

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: file

      - name: Delete stale preview branches
        env:
          NEON_API_KEY: ${{ secrets.NEON_API_KEY }}
          NEON_PROJECT_ID: ${{ vars.NEON_PROJECT_ID }}
        run: |
          if [ -z "$NEON_API_KEY" ] || [ -z "$NEON_PROJECT_ID" ]; then
            echo "::warning::NEON_API_KEY (secret) and/or NEON_PROJECT_ID (variable) are not set — skipping cleanup."
            echo "::warning::See https://neon.tech/docs/guides/branching-github-actions"
            exit 0
          fi
          FLAGS=""
          if [ "${{ inputs.dry_run }}" = "true" ]; then
            FLAGS="--dry-run"
          fi
          bun scripts/cleanup-neon-branches.ts $FLAGS
